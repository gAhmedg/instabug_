apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: instabug-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/instabug-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/instabug-default: "true"
